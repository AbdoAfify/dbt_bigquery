version: 2

models:
  - name: sales_final
    description: "Final sales model with aggregated revenue"
    columns:
      - name: country
        tests:
          - not_null

      - name: total_orders
        tests:
          - not_null
